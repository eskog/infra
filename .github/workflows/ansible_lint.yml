---
name: ansible-lint
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  ansible-lint:
    name: Ansible Lint
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - name: Run ansible-lint
        uses: ansible/ansible-lint@19f673a091111d07688352cd2f19f0460055feba
        with:
          setup_python: true
          python_version: "3.14"
          working_directory: "ansible"
          requirements_file: "requirements.yml"
